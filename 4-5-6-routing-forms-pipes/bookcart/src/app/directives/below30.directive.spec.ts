import { Below30Directive } from './below30.directive';

describe('Below30Directive', () => {
  it('should create an instance', () => {
    const directive = new Below30Directive();
    expect(directive).toBeTruthy();
  });
});
